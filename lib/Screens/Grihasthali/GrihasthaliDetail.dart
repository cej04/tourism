import 'package:flutter/material.dart';

class GrihasthaliDetail extends StatefulWidget {
  const GrihasthaliDetail({ Key? key }) : super(key: key);

  @override
  State<GrihasthaliDetail> createState() => _GrihasthaliDetailState();
}

class _GrihasthaliDetailState extends State<GrihasthaliDetail> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Grihasthali Details Page"),
        
      ),
    );
  }
}